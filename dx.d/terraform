# dx가 source하는 설정. 이미지 태그는 minor 버전 고정, 필요시 갱신.
# shellcheck disable=SC2034
DOCKER_IMAGE="hashicorp/terraform:1.12"
# shellcheck disable=SC2034
DOCKER_OPTS=(
  -v "$HOME/.aws:/root/.aws:ro"
  -v "$HOME/.terraform.d:/root/.terraform.d"
  -e "TF_LOG=${TF_LOG:-}"
)
